# TODO

## Future Enhancements

- **TypeScript migration** -- Rewrite site code in TypeScript and add a build pipeline (tsc/esbuild). Would enable richer diagram generation (typed method signatures, interface relationships) and require swapping acorn for a TS-aware parser (e.g., ts-morph).
